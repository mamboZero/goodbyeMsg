<h1>Miyo's Timeline </h1>
<hr>

<div class="container">
	<% @messages.each do |message| %>
	<h1><%= message.title %></h1>
	<%= link_to 'Know More', message_path(message) %>
	<%#= link_to 'edit', edit_message_path(message) %>
<% end %>
	
</div>

