<h1>Miyo's Timeline </h1>
<hr>

<div>
	<%= link_to '+ Add Message', new_message_path %>
		
	</div>

<div class="container">
	<% @messages.each do |message| %>
	<h1><%= message.title %></h1>
	<%= link_to 'Long Message', message_path(message) %>
	<%#= link_to 'edit', edit_message_path(message) %>
<% end %>
	
</div>

